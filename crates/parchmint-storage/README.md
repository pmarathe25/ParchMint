# parchmint-storage

> Read when changing manifests, paths, locks, bodies, assets, atomic writes,
> transactions, backups, or project open/save.

Owns canonical disk mapping and typed storage failures. Does not own UI policy.
Read [Persistence](../../docs/architecture/persistence.md) and the
[format specifications](../../docs/format/README.md) first.

Check: `cargo test -p parchmint-storage`.
