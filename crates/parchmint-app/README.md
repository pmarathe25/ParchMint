# parchmint-app

> Read when changing workspaces, document sessions, commands, search/statistics
> coordination, recovery, or compile/export workers.

Owns Qt-free application use cases over domain, storage, Markdown, index, and
compile crates. Framework conversion belongs in the bridge. Follow
[Concurrency](../../docs/architecture/concurrency.md) for worker publication.

Check: `cargo test -p parchmint-app`.
