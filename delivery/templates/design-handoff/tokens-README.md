# Design Tokens

Record exported files, Penpot set/theme IDs, export method/tool version, aliases/references, supported token types, and any normalization needed by the deterministic import pipeline. System resolves to Light or Dark at runtime and is not a third value set.
