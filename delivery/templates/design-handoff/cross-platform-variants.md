# Cross-Platform Variants

Record only intentional Windows, macOS, and Linux differences in native menus, dialogs, shortcuts, window chrome, fonts/rendering, input, accessibility, and packaging surfaces. Map each difference to requirement and screen/component IDs; do not duplicate shared boards without need.
