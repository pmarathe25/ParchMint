# Stage 01 Verification Report
Generated: 2026-08-08 01:48:54 UTC
Stage directory: reports/stage001/revision002/

## Stage plan
- `plans/stage-delivery.md` reviewed for process requirements and verification-pass behavior.
- No dedicated Stage 01 plan file was found in `plans/`.

## Checks run

1. `cargo fmt --all --check`
   - Status: PASS

2. `cargo metadata --offline --locked`
   - Status: PASS

3. `cargo test -p parchmint-ci --offline --locked`
   - Status: PASS
   - Result: 10 tests passed, 0 failed
   - Failing tests: none

4. `cargo test --workspace --offline --locked`
   - Status: PASS
   - Result: workspace unit tests and doc-tests passed, 0 failed
   - Failing tests: none

5. `cargo run -p parchmint-ci --offline --locked -- verify`
   - Status: PASS
   - Output: `supply-chain exception and bundled-artifact policies are valid`

6. `cargo run -p parchmint-ci --offline --locked -- sbom verify`
   - Status: PASS
   - Output: `SBOM baseline matches Cargo.lock and bundled-artifacts.toml`

7. `cargo clippy --workspace --offline --locked`
   - Status: PASS
   - Result: no clippy warnings/errors reported
   - Failing lint checks: none

## Workflow/config consistency
- `.github/workflows/ci.yml` was inspected and matches the requested verification surface:
  - `fmt --all --check`
  - `metadata --locked --offline --no-deps`
  - `clippy --workspace --locked --offline -- -D warnings`
  - `test --workspace --locked --offline`
  - `run --package parchmint-ci -- verify`
  - `run --package parchmint-ci -- sbom verify`
- Observed commands are consistent with requested commands; `--no-deps` used only in CI metadata step.
- No GUI verification was executed (not requested by project setup).

