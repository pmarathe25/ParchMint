# Stage 01 Verification Report
Generated: 2026-08-08 01:40:16 UTC
Stage directory: reports/stage001/revision001/

## Checks run

1. `cargo metadata --offline --locked --no-deps --format-version 1`
   - Status: PASS

2. `cargo fmt --all --check`
   - Status: PASS

3. `cargo test -p parchmint-ci --offline --locked`
   - Status: PASS
   - Result: 7 passed, 0 failed
   - Failing tests: none

4. `cargo run -p parchmint-ci --offline --locked -- verify`
   - Status: PASS
   - Output: `supply-chain exception and bundled-artifact policies are valid`

5. `cargo run -p parchmint-ci --offline --locked -- sbom verify`
   - Status: FAIL (command not supported)
   - Error:
     - `supply-chain validation failed: usage: cargo parchmint-ci verify`

## Workflow and SBOM reference validation

- `.github/workflows/ci.yml` includes a SBOM step:
  - `cargo ... run --package parchmint-ci -- sbom verify`.
- `tools/parchmint-ci` only implements `verify` (single subcommand) and parses no `sbom` subcommand.
- Supply-chain references confirmed:
  - `supply-chain/exceptions.toml`
  - `supply-chain/bundled-artifacts.toml`
  - `tools/parchmint-ci` constants: `EXCEPTIONS_PATH`, `ARTIFACTS_PATH`
  - `supply-chain/tools.toml` defines tooling versions including `cargo-cyclonedx`.

## Verifier behavior check

- `cargo fmt`, metadata, unit tests, and offline verification pass indicate Stage 01 post-repair items are largely passing.
- The only remaining CI-facing failure is the workflow’s `sbom verify` invocation, which currently does not match the installed CLI entrypoint behavior.
