#pragma once

// QML type registration includes source headers by basename. Keep this small
// forwarding header while implementation files remain grouped under models/.
#include "models/outline_model.h"
