# Version 1 no-op migration fixture

Format 1 is the only implemented version. This fixture records the current
chain explicitly so a future migration can add `v1 -> v2` without guessing
what an early format-1 project contained.
