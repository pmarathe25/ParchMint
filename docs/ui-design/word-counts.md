# Word counts

- The status bar shows selected-text word count when text is selected, otherwise
  active-document count. Manuscript total remains available. Do not add group,
  Research, or whole-project aggregate counts.
